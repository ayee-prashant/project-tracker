import { requireChatGPTUser } from "../../chatgpt-auth";import { AppShell } from "../../../components/app-shell";import { TicketDetail } from "../../../components/ticket-detail";export const dynamic="force-dynamic";export default async function TicketPage({params}:{params:Promise<{id:string}>}){const {id}=await params,user=await requireChatGPTUser(`/tickets/${id}`);return <AppShell user={user} active="tickets"><TicketDetail id={id}/></AppShell>}
