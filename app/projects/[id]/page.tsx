import { requireChatGPTUser } from "../../chatgpt-auth";import { ProjectDetail } from "../../../components/project-detail";export const dynamic="force-dynamic";export default async function ProjectPage({params}:{params:Promise<{id:string}>}){const {id}=await params;await requireChatGPTUser(`/projects/${id}`);return <ProjectDetail key={id} id={id}/>}
