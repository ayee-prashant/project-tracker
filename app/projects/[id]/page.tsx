import { requireChatGPTUser } from "../../chatgpt-auth";import { AppShell } from "../../../components/app-shell";import { ProjectDetail } from "../../../components/project-detail";export const dynamic="force-dynamic";export default async function ProjectPage({params}:{params:Promise<{id:string}>}){const {id}=await params,user=await requireChatGPTUser(`/projects/${id}`);return <AppShell user={user} active="projects"><ProjectDetail id={id}/></AppShell>}
