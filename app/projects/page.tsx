import { requireChatGPTUser } from "../chatgpt-auth";import { AppShell } from "../../components/app-shell";import { ProjectsWorkspace } from "../../components/projects-workspace";export const dynamic="force-dynamic";export default async function Projects(){const user=await requireChatGPTUser("/projects");return <AppShell user={user} active="projects"><ProjectsWorkspace/></AppShell>}
